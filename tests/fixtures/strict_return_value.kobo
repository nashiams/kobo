// Expression-position @strict: result re-wrapped after guard drops (Trap 14).
struct Pair { a: i32, b: i32 }

fn main() {
    let pair = Pair { a: 1, b: 2 };
    let sum = @strict {
        pair.a + pair.b
    };
}
