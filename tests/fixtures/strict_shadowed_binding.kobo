// Inner let shadows captured binding name.
struct Data { value: i32 }

fn main() {
    let data = Data { value: 42 };
    @strict {
        let data = data.value + 1;  // shadows the captured `data`
        println!("{}", data);
    }
}
