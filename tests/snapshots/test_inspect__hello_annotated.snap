---
source: bin/kobo-cli/tests/test_run.rs
expression: output.stdout
---
fn main() {
// kobo: names @ line 2 -> plain (local-only non-Copy binding)
    let names = vec!["alice", "bob"];
    let count = 2;
    let _count_copy = count;
// kobo: greeting @ line 5 -> plain (local-only non-Copy binding)
    let greeting = greet(names);
    println!("{}", greeting);
}
// kobo: names @ line 9 -> plain (local-only non-Copy binding)
fn greet(names: Vec<&str>) -> String {
    names.join(", ")
}
