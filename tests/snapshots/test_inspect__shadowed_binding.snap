---
source: bin/kobo-cli/tests/test_run.rs
expression: output.stdout
---
fn main() {
// kobo: x @ line 2 -> plain (local-only non-Copy binding)
    let x = String::from("outer");
    println!("{}", x.len());
    println!("{}", x.len());
    {
// kobo: x @ line 7 -> plain (local-only non-Copy binding)
        let mut x: Vec<String> = Vec::new();
        x.push(String::from("inner"));
        println!("{}", x.len());
    }
}
