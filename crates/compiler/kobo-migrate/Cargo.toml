[package]
name    = "kobo-migrate"
version = "0.1.0"
edition = "2021"

[dependencies]
kobo-ir     = { path = "../kobo-ir" }
kobo-errors = { path = "../kobo-errors" }

[dev-dependencies]
proptest = "1"

[[bench]]
name = "scale"
harness = false
