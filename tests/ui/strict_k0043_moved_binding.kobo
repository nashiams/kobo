fn main() {
    let data = kobo::bind(vec![1, 2, 3]);
    let alias = data.clone();
    let _ = data.borrow().len();
    @strict {
        consume(data);
    }
}

fn consume(_: Rc<RefCell<Vec<i32>>>) {}
