fn main() {
    let local_only = vec![1, 2, 3];
    local_only.push(4);
    println!("{}", local_only.len());

    let shared = vec![10, 20];
    shared.len();
    shared.len();
    shared.is_empty();
}
