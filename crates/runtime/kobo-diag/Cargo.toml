[package]
name    = "kobo-diag"
version = "0.4.0"
edition = "2021"
publish = true

# Ships to crates.io and is used by generated .rs files at runtime.
# Zero kobo-* dependencies.
[features]
diag = []

[dependencies]

[dev-dependencies]
static_assertions = "1"
